<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fiek.transfuzioni_gjakut">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ShtoMarres">


        </activity>
        <activity android:name=".splashScreen"></activity>
        <activity android:name=".Home">


        </activity>
        <activity android:name=".activities.Coronavirus_Cases" />
        <activity android:name=".activities.Registration_form">


        </activity>
        <activity android:name=".forms.RegistrationForm" />
        <activity android:name=".forms.LoginForm">

        </activity>
        <activity android:name=".ShtoDhurues">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".MainActivity">


        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>